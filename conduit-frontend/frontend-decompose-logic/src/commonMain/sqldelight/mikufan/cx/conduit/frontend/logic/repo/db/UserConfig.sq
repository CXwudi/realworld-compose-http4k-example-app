
getSingleConfig:
SELECT * FROM userConfig WHERE id = 1;

setUrl:
UPDATE userConfig SET url = ? WHERE id = 1;

setUsername:
UPDATE userConfig SET username = ? WHERE id = 1;

setToken:
UPDATE userConfig SET token = ? WHERE id = 1;
