package mikufan.cx.conduit.frontend.logic.repo.db

import org.koin.dsl.module

// just an empty module, but the driver will be provided in the js-app main function
internal actual val dbDriverProvider = module { }